export type Animal = {
    id: number;
    name: string;
    latinName: string;
    yearOfBirth: number;
    shortDescription: string;
    imageUrl: string;
    medicine: string;
    isFed: boolean;
    lestFed: string; // Datum och tid
}